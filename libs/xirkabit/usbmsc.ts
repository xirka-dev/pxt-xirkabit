namespace control {
  /**
   * Adds mass storage interface to USB stack, exposing the internal 
   * flash as a drive.
   */
  //% blockId=control_enable_usbmsc block="enable USB MSC"
  //% shim=control::enableUsbMsc
  export function enableUsbMsc() {
    return;
  }
}
